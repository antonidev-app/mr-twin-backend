meta {
  name: Webhooks
  seq: 6
}

docs {
  Server-to-server callbacks from payment providers — no bearer auth,
  verified by signature instead. In real usage these are called by
  Midtrans itself, not from Bruno; the request here is a reference
  example for manual testing against your own local backend.
}
