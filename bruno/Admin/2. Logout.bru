meta {
  name: 2. Logout
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/admin/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

docs {
  Revokes the current access token (currentAccessToken()->delete()).
}
